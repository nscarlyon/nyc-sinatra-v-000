<!DOCTYPE html>
<html>
  <head>

  </head>
  <body>
<form action="/figures/<%= @figure.id %>" method="POST">
<input type="hidden" name="_method" value="PATCH">

  <p><b>Figure Name:</b> <input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>"></p>

  <p><b>Existing Landmarks</b></p>
  <% Landmark.all.each do |landmark| %>
  <%= landmark.name %> <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%= landmark.id %>"
  value="<%= landmark.id %>"   <%='checked' if @figure.landmarks.include?(landmark) %>>
  <%end%>

  <p><b>Existing Titles</b></p>
  <% Title.all.each do |title| %>
  <%= title.name %> <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>"
  value="<%= title.id %>"   <%='checked' if @figure.titles.include?(title) %>>
  <%end%>

  <p><b>Create New Landmark and/or Title</b></p>
  <p>Landmark Name: <input type="text" name="landmark[name]" id="new_landmark"></p>
  <p>Title Name: <input type="text" name="title[name]" id="new_title"></p>
  <p><input type="submit" value="Edit Figure"></p>
  </body>

</html>
